# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
build/
dist/

# Editor/OS
.vscode/
.idea/
*.swp
*~
.DS_Store

# Project artefacts
*.deb
*.rpm
*.tar.gz
*.tgz

# Local notes / scratch
note
notes/
TODO.local

# Site-specific config (must NOT be committed — contains internal hostnames)
/etc/dtu-setup/
site.conf
*.env
# Site profiles with real DTU values are distributed out-of-band — never commit.
# examples/ holds only generic templates (*.example); the department profiles
# dtu-sustain.env / dtu-ait.env were removed from the repo in v1.5.0.


# Cisco redistributables (proprietary — never commit)
cisco-secure-client*

# Udfyldt print.conf til det frittstående printerscript. Indeholder
# interne værtsnavne — kun print.conf.example hører i repoet.
scripts/standalone/print.conf
